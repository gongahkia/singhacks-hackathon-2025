{
  "name": "hedera-agent-contracts",
  "version": "1.0.0",
  "description": "Smart contracts for Hedera Agent Economy",
  "scripts": {
    "compile": "npx hardhat compile",
    "deploy": "npx hardhat run deploy/deploy.js --network hedera_testnet",
    "test": "npx hardhat test"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-ethers": "^3.0.0",
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.19.0",
    "ethers": "^6.9.0",
    "dotenv": "^16.3.1"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.0"
  }
}

